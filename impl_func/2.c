#include <stdio.h>

// Función para determinar si un número es par (1) o impar (0)
int esPar(int num) {
    // Completa la función aquí
}

int main() {
    int numero = 17;
    if (esPar(numero))
        printf("%d es un número par.\n", numero);
    else
        printf("%d es un número impar.\n", numero);
    return 0;
}

